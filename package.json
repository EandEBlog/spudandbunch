{
  "name": "spudandbunch",
  "version": "0.0.0",
  "private": true,
  "description": "Spud & Bunch — a dockerized food, cooking & travel blog (Astro + Strapi).",
  "type": "module",
  "comment:workspaces": "apps/cms (Strapi) is intentionally excluded — it is a self-contained, dockerized service that manages its own dependencies inside its container. Node services we lint/test/build (web, and later rebuild-hook/preview) are listed explicitly.",
  "workspaces": [
    "apps/web"
  ],
  "engines": {
    "node": ">=20.3.0"
  },
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "preview": "npm run preview -w apps/web",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@vitest/coverage-v8": "^2.1.9",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-astro": "^1.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.4.1",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.16.0",
    "vitest": "^2.1.6"
  }
}
